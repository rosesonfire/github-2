/* eslint-disable no-undef */

import { expect } from './setup'

describe('Dummy test', () => {
  describe('When doing dummy test', () => {
    it('should do nothing', () =>
      expect(undefined).to.be.undefined
    )
  })
})
